This package/language was successfully configured, compiled, and installed. The checks, which were attempted prior to installation, failed, but it is unknown whether or not said checks would work now that the package has been installed.
