This package did not require any compilation, rather, it required numerous commands, which moved/linked different files and directories. The current installation has not been confirmed to be working, but, given how itstool compiled correctly (which requires this package), one could only assume that this package has, in fact, been installed correctly. The final portion of the installation guide, which explains how to configure this package, was never executed. Due to the aforementioned configuration having never been executed, the extracted (and current build) folder for this package will remain on the system, until it can be confirmed that this package is stable.
